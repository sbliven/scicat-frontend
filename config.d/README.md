# config.d directory

Any .yaml, .yml, or .json files in this directory will be merged alphabetically to
create custom-config.json file.

## Running

```
npm run generate:config
```

This will produce `../src/assets/custom-config.json`
